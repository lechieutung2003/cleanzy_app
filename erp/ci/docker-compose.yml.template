version: '3.1'

services:

  web:
    build: .
    restart: always
    ports:
      - __WEB_PORT__:8009
    env_file:
      - ./docker.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
